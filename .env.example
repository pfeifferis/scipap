# OpenRouter API Key (can also be configured directly in the Web UI Settings)
OPENROUTER_API_KEY=

# Default LLM Model on OpenRouter (50% batch discount pricing)
DEFAULT_MODEL=google/gemini-2.5-flash-lite:batch
